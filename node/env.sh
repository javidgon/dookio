# The IP ADDRESS of the proxy e.g http://123.123.123.123
export DOOKIO_PROXY_ADDRESS="localhost"
# The linux user of the proxy machine e.g root
export DOOKIO_PROXY_USER="root"
# The root of the proxy machine, that will be used for fetching app's data  e.g /home/michael
export DOOKIO_PROXY_ROOT="/home/git"
# The root of the node machine, that will be used for storing app's data  e.g /root
export DOOKIO_NODE_ROOT="/tmp"
